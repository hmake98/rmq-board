<header>
    <div class="header-content">
        <h1>RabbitMQ Board</h1>
        <div class="connection-indicators">
            <div class="connection-indicator">
                <span class="indicator-label">HTTP API:</span>
                <span id="http-status" class="indicator active">Connected</span>
            </div>
            <div class="connection-indicator">
                <span class="indicator-label">AMQP:</span>
                <span id="amqp-status" class="indicator">Connecting...</span>
            </div>
        </div>
        <button class="mobile-menu-button button button-secondary button-sm">Menu</button>
    </div>
    <div class="mobile-connection-status">
        <div class="connection-indicator">
            <span class="indicator-label">HTTP:</span>
            <span id="mobile-http-status" class="indicator active">Connected</span>
        </div>
        <div class="connection-indicator">
            <span class="indicator-label">AMQP:</span>
            <span id="mobile-amqp-status" class="indicator">Connecting...</span>
        </div>
    </div>
</header>